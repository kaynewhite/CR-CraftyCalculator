Please review the Product Pricing Calculator component and its associated CSS/Tailwind styles. I want to perform a final UI polish to make the page look completely perfect, premium, and flawless. Please implement the following design refinements:

Typography & Hierarchy: Ensure all labels use a consistent font weight (e.g., font-medium or font-semibold in Tailwind) and that text colors have high contrast against the dark background. The placeholder text (like 'e.g., Custom Gift Card Set') should be distinctly muted compared to the active user text input.

Form Layout & Alignment: Ensure all input fields, dropdown select boxes, and currency prefix add-ons (₱) have matching heights, border-radii (rounded-md or rounded-lg), focus ring states (focus:ring-2), and consistent horizontal/vertical padding. Align the prefix currency symbol flawlessly inside or attached to the numeric inputs.

Buttons & Interactivity: Update the action buttons (Calculate Pricing, Save Pricing, Reset). Give them clear visual hierarchy—'Calculate' should be a prominent primary action button with a subtle hover transition state, while 'Reset' should look like a clean, minimal secondary or tertiary outline button.

Results Card Component: Style the placeholder state on the right-hand panel ('Click Calculate to see results') so it looks highly professional. When results are calculated, present the breakdown (Total Material Cost, Production Cost, Suggested Retail Price, Net Profit) using clean dividers, bold key numbers, and a highlighted final price badge.

Mobile Responsiveness Check: Double-check the grid layout logic. On small mobile screen sizes, ensure the left input form and the right results card stack vertically with perfect spacing (space-y-6 or gap-6) and clear margin padding on the sides so no elements clip out of the screen edge.

Limit Badge Visuals: Style the Calculations remaining: 3/3 badge to look like a clean, small pill tag (e.g., subtle amber or gray background with crisp text) tucked neatly near the calculation actions.

Let's make this page look like a world-class SaaS application! Let me know once you've applied these layout enhancements.